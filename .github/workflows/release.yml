# Upstream Start9 marketplace release pipeline...
#
# Disabled for personal use: only `workflow_dispatch` (manual) trigger is
# active. To re-enable for an upstream adoption, replace the `on:` block
# below with the original:
#
#   on:
#     push:
#       tags:
#         - 'v*.*'

name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: start9labs/shared-workflows/.github/workflows/release.yml@master
    with:
      # FREE_DISK_SPACE: true
      RELEASE_REGISTRY: ${{ vars.RELEASE_REGISTRY }}
      S3_S9PKS_BASE_URL: ${{ vars.S3_S9PKS_BASE_URL }}
    secrets:
      DEV_KEY: ${{ secrets.DEV_KEY }}
      S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
      S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
    permissions:
      contents: write
